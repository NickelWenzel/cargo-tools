[package]
name = "iced_viewless"
version = "0.1.0"
edition = "2024"
authors = ["Nicolas Wenzel"]
description = "Viewless application runtime for iced, enabling background services without UI"
license = "MIT"
repository = "https://github.com/NickelWenzel/cargo-tools/tree/master/packages/iced_viewless"
documentation = "https://docs.rs/iced_viewless"
exclude = ["tests/"]
keywords = ["iced", "headless", "daemon", "runtime", "async"]
categories = ["asynchronous"]

[dependencies]
iced_futures = { workspace = true }
iced_runtime = { workspace = true }
futures = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }

tokio = { workspace = true, optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen-futures = { workspace = true }

[dev-dependencies]
wasm-bindgen-test = { workspace = true }
tracing = { workspace = true }
tracing-test = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
iced_viewless = { workspace = true }
tokio = { workspace = true, features = ["rt", "macros", "time"] }

[features]
default = ["thread-pool"]
tokio = ["dep:tokio", "iced_futures/tokio"]
smol = ["iced_futures/smol"]
thread-pool = ["iced_futures/thread-pool"]
